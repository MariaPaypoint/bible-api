services:
  bible-api:
    container_name: bible-api
    build: .
    ports:
      - "8000:8000"
      - "80:8000"
    env_file:
      - app.env
    restart: always
    networks:
      - mysql_default

networks:
  mysql_default:
    external: true
